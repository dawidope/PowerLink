<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PowerLink.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerLink.App"
    Title="PowerLink">

    <NavigationView x:Name="Nav"
                    IsBackButtonVisible="Collapsed"
                    IsSettingsVisible="False"
                    PaneDisplayMode="Left"
                    OpenPaneLength="200"
                    Background="Transparent"
                    SelectionChanged="Nav_SelectionChanged">
        <NavigationView.MenuItems>
            <NavigationViewItem Content="Deduplicate" Tag="dedup" IsSelected="True">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Copy"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Clone" Tag="clone">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Folder"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Junction" Tag="junction">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Link"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Inspector" Tag="inspector">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Find"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem Content="Settings" Tag="settings">
                <NavigationViewItem.Icon>
                    <SymbolIcon Symbol="Setting"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <NavigationView.FooterMenuItems>
            <NavigationViewItem x:Name="ThemeToggleItem"
                                Content="Dark mode"
                                SelectsOnInvoked="False"
                                Tapped="ThemeToggleItem_Tapped"
                                ToolTipService.ToolTip="Toggle light / dark theme">
                <NavigationViewItem.Icon>
                    <FontIcon x:Name="ThemeToggleIcon"
                              FontFamily="{StaticResource SymbolThemeFontFamily}"
                              Glyph="&#xE706;"/>
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.FooterMenuItems>

        <Frame x:Name="ContentFrame" Background="Transparent"/>
    </NavigationView>
</Window>
